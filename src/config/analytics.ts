// GoatCounter site code — the subdomain in https://{code}.goatcounter.com.
// Leave empty to disable view/visitor counting entirely (tracking script and
// counter displays both no-op when this is blank).
export const GOATCOUNTER_CODE = 'jhwanseok';
